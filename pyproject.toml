[project]
name = "eot"
version = "0.1.0"
description = "End-of-turn detector for voice agents: pause-level training, causal policy, EoT Bench adapter, ONNX serving."
requires-python = ">=3.11,<3.14"
dependencies = [
    "numpy>=1.26",
    "torch>=2.3",
    "transformers>=4.44",
    "soundfile>=0.12",
    "onnxruntime>=1.18",
    "onnx>=1.16",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "pydantic>=2.7",
]

[project.optional-dependencies]
data = [
    "datasets>=3.0",
    "pyarrow>=16",
]
dev = [
    "pytest>=8",
]
tracking = [
    "wandb>=0.29.0",
]

[project.scripts]
eot-acquire = "eot.acquire:main"
eot-mine = "eot.prefix_mining:main"
eot-train = "eot.train:main"
eot-export = "eot.export_onnx:main"
eot-serve = "eot.serve:main"
eot-loadtest = "eot.load_test:main"
eot-sweep = "eot.policy:main"
eot-challenge = "eot.challenge:main"
eot-benchmark-mfu = "eot.benchmark_mfu:main"
eot-apptek = "eot.apptek:main"
eot-krisp = "eot.krisp:main"
eot-qa = "eot.labeling_qa:main"
eot-report = "eot.report:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eot"]

[tool.uv]
package = true

[tool.pytest.ini_options]
testpaths = ["tests"]
