# Credentials are needed only for data acquisition and training; inference and evaluation need none.
# Copy to .env (ignored by Git) and fill in what you use.
HF_TOKEN=            # Hugging Face token: gated pretrained snapshots (CohereLabs/cohere-transcribe-03-2026)
WANDB_API_KEY=       # optional: Weights & Biases tracking for eot-train / eot-train-backbone
