"""Endpointing policy, benchmark adapters and reports. ``eot.eval.policy``, ``eot.eval.krisp`` and
``eot.eval.eotbench`` (ONNX backend) are torch-free; ``eot.eval.eotbench.batched_predictions`` is
the shared adapter batching used by every scorer."""
